# 滇藏骑行 · 微信小程序技术方案

> 版本 v1.0 | 2026-04-28

---

## 一、技术选型

| 层级 | 技术方案 | 选型理由 |
|------|---------|---------|
| 框架 | 微信小程序原生 + TypeScript | 性能最优，Canvas 控制力强，TS 保证代码质量 |
| UI 组件 | 自研轻量组件 | 游戏类 UI 高度自定义，不依赖第三方库 |
| 渲染引擎 | Canvas 2D（地图 + 关卡）+ WXML（UI 层） | 双层架构，游戏画面用 Canvas，弹窗/HUD 用原生组件 |
| 后端 | 微信云开发（CloudBase） | 零运维、免域名备案、与小程序天然集成 |
| 数据库 | 云数据库（MongoDB 文档型）| 灵活 Schema，适合游戏存档的嵌套数据 |
| 存储 | 云存储 | 分享图、骑手形象素材 |
| 函数 | 云函数（Node.js 18）| 步数解密、防作弊校验、排行榜计算 |

---

## 二、项目结构

```
dianzang-ride/
├── project.config.json          # 小程序项目配置
├── app.ts                       # 全局入口
├── app.json                     # 页面路由 & 全局配置
├── app.wxss                     # 全局样式（暗色主题变量）
│
├── pages/
│   ├── splash/                  # 启动页
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   └── index.ts
│   ├── customize/               # 骑手自定义
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   └── index.ts
│   ├── map/                     # 主地图（核心页面）
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   ├── index.ts
│   │   └── map-renderer.ts     # Canvas 地图渲染器
│   ├── riding/                  # 骑行动画
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   └── index.ts
│   ├── station/                 # 站点详情
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   └── index.ts
│   └── profile/                 # 个人中心
│       ├── index.wxml
│       ├── index.wxss
│       └── index.ts
│
├── games/                       # 小关卡（独立模块）
│   ├── base-game.ts             # 关卡基类
│   ├── rock-dodge/              # 落石躲避
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   ├── index.ts
│   │   └── renderer.ts
│   ├── dog-chase/               # 野狗追咬
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   ├── index.ts
│   │   └── renderer.ts
│   ├── avalanche/               # 雪崩
│   │   └── ...
│   ├── tire-fix/                # 爆胎修补
│   │   └── ...
│   └── wind-balance/            # 横风平衡
│       └── ...
│
├── components/                  # 公共组件
│   ├── hud/                     # 顶部状态栏（体能/里程/心情/耐久）
│   ├── event-toast/             # 事件触发弹窗
│   ├── rider-avatar/            # 骑手头像组件
│   ├── station-card/            # 站点信息卡片
│   ├── ad-modal/                # 广告复活弹窗
│   └── share-card/              # 分享图生成组件
│
├── services/                    # 业务逻辑层
│   ├── step-service.ts          # 步数获取 & 体能转化
│   ├── ride-service.ts          # 骑行里程计算
│   ├── event-service.ts         # 随机事件触发引擎
│   ├── rider-service.ts         # 骑友数据（地图上他人位置）
│   ├── equip-service.ts         # 装备管理
│   └── ad-service.ts            # 广告管理
│
├── models/                      # 数据模型（TypeScript 类型）
│   ├── player.ts
│   ├── station.ts
│   ├── event.ts
│   ├── equipment.ts
│   └── rider.ts
│
├── utils/
│   ├── cloud.ts                 # 云函数调用封装
│   ├── storage.ts               # 本地缓存管理
│   ├── canvas-helper.ts         # Canvas 绘制工具函数
│   └── math.ts                  # 游戏数值计算
│
├── assets/
│   ├── images/                  # 本地图片（icon、小素材）
│   ├── sprites/                 # 关卡精灵图
│   └── data/
│       ├── stations.json        # 站点数据
│       └── events.json          # 事件配置
│
└── cloud/                       # 云函数目录
    ├── getSteps/                # 解密步数
    │   ├── index.js
    │   └── package.json
    ├── syncProgress/            # 同步游戏进度
    │   └── index.js
    ├── getRiders/               # 获取路线上骑友
    │   └── index.js
    ├── getRanking/              # 排行榜
    │   └── index.js
    └── genShareImage/           # 生成分享图
        └── index.js
```

---

## 三、数据库设计

### 3.1 集合（Collection）总览

| 集合名 | 说明 | 预估数据量 |
|--------|------|-----------|
| `players` | 玩家存档 | 1 条/人 |
| `daily_steps` | 每日步数记录 | 1 条/人/天 |
| `rider_positions` | 骑友实时位置（地图展示） | 1 条/人 |
| `rankings` | 排行榜快照 | 周更新 |
| `events_log` | 事件记录（数据分析用） | N 条/人 |

### 3.2 players（玩家存档）

```json
{
  "_id": "openid_xxx",
  "nickname": "骑行侠小张",
  "avatarUrl": "https://...",

  // 骑手自定义
  "rider": {
    "helmet": 0,
    "jersey": 2,
    "bike": 1,
    "bikeColor": "#4fd1c5",
    "titleFrame": "default",
    "title": "新手骑手"
  },

  // 核心进度
  "progress": {
    "day": 5,
    "km": 420,
    "currentStation": 3,
    "startDate": "2026-04-20"
  },

  // 属性
  "stats": {
    "energy": 130,
    "energyReserve": 80,
    "mood": 85,
    "durability": 92,
    "altitudeAdapt": 15
  },

  // 装备
  "equipment": [
    { "id": "mountain_bike", "durability": 92 },
    { "id": "helmet_basic", "durability": 100 },
    { "id": "knee_guard", "durability": 100 }
  ],

  // 背包
  "inventory": {
    "coins": 350,
    "items": [
      { "id": "energy_drink", "count": 2 },
      { "id": "dog_repeller", "count": 1 }
    ]
  },

  // 成就
  "achievements": ["first_ride", "reach_lijiang"],

  // 广告相关
  "adState": {
    "reviveToday": 0,
    "energyAdToday": 0,
    "lastAdDate": "2026-04-28"
  },

  "createdAt": "2026-04-20T10:00:00Z",
  "updatedAt": "2026-04-28T08:30:00Z"
}
```

### 3.3 daily_steps（每日步数）

```json
{
  "_id": "openid_xxx_20260428",
  "openid": "openid_xxx",
  "date": "2026-04-28",
  "rawSteps": 12350,
  "energyConverted": 123,
  "energyUsed": 80,
  "kmRidden": 95,
  "createdAt": "2026-04-28T07:00:00Z"
}
```

### 3.4 rider_positions（骑友位置）

```json
{
  "_id": "openid_xxx",
  "nickname": "骑行侠小张",
  "rider": { "helmet": 0, "jersey": 2, "bike": 1, "bikeColor": "#4fd1c5" },
  "title": "高原老手",
  "km": 420,
  "currentStation": 3,
  "visible": true,
  "updatedAt": "2026-04-28T08:30:00Z"
}
```

### 3.5 索引设计

| 集合 | 索引 | 用途 |
|------|------|------|
| `rider_positions` | `{ currentStation: 1 }` | 按站点查骑友 |
| `rider_positions` | `{ km: -1 }` | 里程排行 |
| `daily_steps` | `{ openid: 1, date: -1 }` | 查玩家历史步数 |
| `rankings` | `{ type: 1, week: 1 }` | 周排行榜查询 |

---

## 四、核心流程 & API 设计

### 4.1 每日步数获取 → 体能转化

```
时序：用户打开小程序 → 前端授权 → 获取加密步数 → 云函数解密 → 更新体能
```

**前端调用：**

```typescript
// services/step-service.ts
export async function syncDailySteps(): Promise<StepResult> {
  // 1. 获取微信运动数据（需用户授权 scope.werun）
  const weRunRes = await wx.getWeRunData();

  // 2. 调用云函数解密
  const res = await wx.cloud.callFunction({
    name: 'getSteps',
    data: {
      encryptedData: weRunRes.encryptedData,
      iv: weRunRes.iv,
    },
  });

  const { steps, energy } = res.result as StepResult;
  return { steps, energy };
}
```

**云函数：getSteps**

```javascript
// cloud/getSteps/index.js
const cloud = require('wx-server-sdk');
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });

exports.main = async (event, context) => {
  const { OPENID } = cloud.getWXContext();
  const db = cloud.database();

  // 1. 解密步数
  const stepData = await cloud.getOpenData({
    list: [{
      encryptedData: event.encryptedData,
      iv: event.iv,
    }],
  });

  const stepList = stepData.list[0].stepInfoList;
  const todaySteps = stepList[stepList.length - 1].step;

  // 2. 转化体能（每 100 步 = 1 体能，上限 200）
  const energy = Math.min(Math.floor(todaySteps / 100), 200);

  // 3. 查询今日是否已记录
  const today = new Date().toISOString().slice(0, 10);
  const dailyId = `${OPENID}_${today.replace(/-/g, '')}`;

  const existing = await db.collection('daily_steps').doc(dailyId).get()
    .catch(() => null);

  if (!existing) {
    // 4. 写入每日步数记录
    await db.collection('daily_steps').add({
      data: {
        _id: dailyId,
        openid: OPENID,
        date: today,
        rawSteps: todaySteps,
        energyConverted: energy,
        energyUsed: 0,
        kmRidden: 0,
        createdAt: db.serverDate(),
      },
    });

    // 5. 更新玩家体能
    await db.collection('players').doc(OPENID).update({
      data: {
        'stats.energy': _.inc(energy),
        updatedAt: db.serverDate(),
      },
    });
  }

  return { steps: todaySteps, energy, isNew: !existing };
};
```

### 4.2 骑行里程计算

```typescript
// services/ride-service.ts

interface RideConfig {
  currentKm: number;
  targetKm: number;           // 下一站点距离
  elevation: number;          // 当前海拔
  energy: number;             // 可用体能
  altitudeAdapt: number;      // 高原适应度 (0-50)
  hasKneeGuard: boolean;      // 是否有护膝
}

export function calculateRide(config: RideConfig) {
  const { elevation, energy, altitudeAdapt, hasKneeGuard } = config;

  // 海拔消耗系数
  let costMultiplier = 1.0;
  if (elevation > 4000) costMultiplier = 1.5;
  else if (elevation > 3000) costMultiplier = 1.2;

  // 高原适应度减免（最多减 50%）
  costMultiplier *= (1 - altitudeAdapt / 100);

  // 护膝减免 10%
  if (hasKneeGuard) costMultiplier *= 0.9;

  // 每体能点可推进的公里数
  const kmPerEnergy = elevation > 3500 ? 0.5
    : elevation > 3000 ? 1.0
    : 2.0;

  // 本次最大可骑行公里数
  const maxKm = Math.floor(energy / costMultiplier * kmPerEnergy);
  const distToNext = config.targetKm - config.currentKm;
  const actualKm = Math.min(maxKm, distToNext, 80); // 单次上限 80km

  // 实际消耗体能
  const energyCost = Math.ceil(actualKm / kmPerEnergy * costMultiplier);

  return { actualKm, energyCost, costMultiplier };
}
```

### 4.3 随机事件触发引擎

```typescript
// services/event-service.ts

interface EventTriggerContext {
  currentKm: number;
  lastEventKm: number;       // 上次触发事件的位置
  stationIndex: number;
  elevation: number;
  mood: number;
}

// 事件池
const EVENT_POOL: GameEvent[] = [
  { id: 'rock',      name: '落石',     minKm: 0,    zones: [2,3,4,5,6,7,8], weight: 30, game: 'rock-dodge' },
  { id: 'dog',       name: '野狗追咬', minKm: 0,    zones: [0,1,2,4,5,9],   weight: 25, game: 'dog-chase' },
  { id: 'avalanche', name: '雪崩',     minKm: 400,  zones: [3,8],           weight: 10, game: 'avalanche' },
  { id: 'blizzard',  name: '暴风雪',   minKm: 300,  zones: [2,3],           weight: 15, game: null },
  { id: 'wind',      name: '横风',     minKm: 600,  zones: [5,6,7,8,12],    weight: 20, game: 'wind-balance' },
  { id: 'flat_tire', name: '爆胎',     minKm: 0,    zones: 'all',           weight: 20, game: 'tire-fix' },
  { id: 'eagle',     name: '鹰袭',     minKm: 800,  zones: [6,7,8,12],      weight: 10, game: null },
  { id: 'mudslide',  name: '泥石流',   minKm: 1100, zones: [10],            weight: 15, game: 'avalanche' },
  { id: 'tibetan',   name: '遇到藏民', minKm: 300,  zones: [3,4,5,9,11],    weight: 20, game: null },
  { id: 'supply',    name: '补给站',   minKm: 0,    zones: 'all',           weight: 25, game: null },
];

export function tryTriggerEvent(ctx: EventTriggerContext): GameEvent | null {
  const distSinceLast = ctx.currentKm - ctx.lastEventKm;

  // 每 30~50km 触发一次
  if (distSinceLast < 30) return null;
  const triggerChance = Math.min((distSinceLast - 30) / 20, 1.0);

  // 心情值影响：心情越低，负面事件概率越高
  const moodFactor = ctx.mood < 50 ? 1.3 : 1.0;

  if (Math.random() > triggerChance * moodFactor) return null;

  // 筛选当前区段可触发的事件
  const candidates = EVENT_POOL.filter(e => {
    if (ctx.currentKm < e.minKm) return false;
    if (e.zones === 'all') return true;
    return e.zones.includes(ctx.stationIndex);
  });

  if (candidates.length === 0) return null;

  // 加权随机抽取
  const totalWeight = candidates.reduce((s, e) => s + e.weight, 0);
  let rand = Math.random() * totalWeight;
  for (const event of candidates) {
    rand -= event.weight;
    if (rand <= 0) return event;
  }

  return candidates[candidates.length - 1];
}
```

### 4.4 获取路线骑友（地图显示）

**云函数：getRiders**

```javascript
// cloud/getRiders/index.js
const cloud = require('wx-server-sdk');
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });

exports.main = async (event) => {
  const { OPENID } = cloud.getWXContext();
  const db = cloud.database();
  const _ = db.command;

  // 获取所有可见骑友，按站点分组
  const { data: riders } = await db.collection('rider_positions')
    .where({
      visible: true,
      _id: _.neq(OPENID),  // 排除自己
    })
    .orderBy('km', 'desc')
    .limit(100)
    .get();

  // 按站点分组
  const grouped = {};
  for (const rider of riders) {
    const station = rider.currentStation;
    if (!grouped[station]) grouped[station] = [];
    grouped[station].push({
      nickname: rider.nickname,
      rider: rider.rider,
      title: rider.title,
      km: rider.km,
    });
  }

  return { riders: grouped, total: riders.length };
};
```

---

## 五、关卡 Canvas 渲染架构

### 5.1 基类设计

```typescript
// games/base-game.ts

export abstract class BaseGame {
  protected ctx: CanvasRenderingContext2D;
  protected width: number;
  protected height: number;
  protected running = false;
  protected frameId: number = 0;

  // 子类实现
  abstract onInit(): void;
  abstract onUpdate(dt: number): void;
  abstract onRender(): void;
  abstract onTouchStart(x: number, y: number): void;
  abstract onTouchMove(x: number, y: number): void;
  abstract onTouchEnd(x: number, y: number): void;

  constructor(canvas: WechatMiniprogram.Canvas) {
    this.ctx = canvas.getContext('2d')!;
    this.width = canvas.width;
    this.height = canvas.height;
  }

  start() {
    this.running = true;
    this.onInit();
    this.loop(0);
  }

  stop() {
    this.running = false;
  }

  private lastTime = 0;
  private loop = (time: number) => {
    if (!this.running) return;
    const dt = time - this.lastTime;
    this.lastTime = time;

    this.onUpdate(dt);
    this.ctx.clearRect(0, 0, this.width, this.height);
    this.onRender();

    this.frameId = this.ctx.canvas.requestAnimationFrame(this.loop);
  };
}
```

### 5.2 落石关卡示例

```typescript
// games/rock-dodge/renderer.ts

import { BaseGame } from '../base-game';

interface Rock {
  x: number; y: number;
  lane: number; speed: number;
  hit: boolean;
}

export class RockDodgeGame extends BaseGame {
  private playerLane = 1;        // 0/1/2 三条车道
  private laneWidth = 0;
  private rocks: Rock[] = [];
  private hp = 100;
  private score = 0;
  private wave = 1;
  private touchStartX = 0;

  // 回调
  onWin: ((score: number) => void) | null = null;
  onLose: (() => void) | null = null;

  onInit() {
    this.laneWidth = this.width / 3;
    this.spawnWave();
  }

  onUpdate(dt: number) {
    for (const rock of this.rocks) {
      if (rock.hit) continue;
      rock.y += rock.speed * (dt / 16);

      // 碰撞检测
      if (rock.y > this.height - 120 && rock.y < this.height - 40
          && rock.lane === this.playerLane) {
        rock.hit = true;
        this.hp -= 15;
        if (this.hp <= 0) {
          this.stop();
          this.onLose?.();
          return;
        }
      }

      // 成功躲避
      if (rock.y > this.height && !rock.hit) {
        this.score += 5;
      }
    }

    // 清理出界石头
    this.rocks = this.rocks.filter(r => r.y < this.height + 50);
  }

  onRender() {
    const { ctx, width, height, laneWidth } = this;

    // 背景
    const grad = ctx.createLinearGradient(0, 0, 0, height);
    grad.addColorStop(0, '#1a202c');
    grad.addColorStop(1, '#2d3748');
    ctx.fillStyle = grad;
    ctx.fillRect(0, 0, width, height);

    // 车道分隔线
    ctx.strokeStyle = 'rgba(255,255,255,0.05)';
    ctx.lineWidth = 1;
    for (let i = 1; i < 3; i++) {
      ctx.beginPath();
      ctx.moveTo(i * laneWidth, 0);
      ctx.lineTo(i * laneWidth, height);
      ctx.stroke();
    }

    // 石头
    ctx.font = '32px serif';
    for (const rock of this.rocks) {
      if (!rock.hit) {
        ctx.fillText('🪨', rock.lane * laneWidth + laneWidth / 2 - 16, rock.y);
      }
    }

    // 骑手
    ctx.font = '36px serif';
    const px = this.playerLane * laneWidth + laneWidth / 2 - 18;
    ctx.fillText('🚴', px, height - 80);
  }

  onTouchStart(x: number) { this.touchStartX = x; }
  onTouchMove() {}
  onTouchEnd(x: number) {
    const dx = x - this.touchStartX;
    if (Math.abs(dx) < 30) return;
    if (dx < 0 && this.playerLane > 0) this.playerLane--;
    if (dx > 0 && this.playerLane < 2) this.playerLane++;
  }

  private spawnWave() {
    const speeds = [3, 4.5, 6];
    const counts = [6, 8, 12];
    const speed = speeds[Math.min(this.wave - 1, 2)];
    const count = counts[Math.min(this.wave - 1, 2)];

    let spawned = 0;
    const interval = setInterval(() => {
      if (spawned >= count || !this.running) {
        clearInterval(interval);
        // 等 2 秒后进入下一波或结束
        setTimeout(() => {
          if (this.wave < 3) {
            this.wave++;
            this.spawnWave();
          } else {
            this.stop();
            this.onWin?.(this.score);
          }
        }, 2000);
        return;
      }
      this.rocks.push({
        x: 0, y: -40,
        lane: Math.floor(Math.random() * 3),
        speed, hit: false,
      });
      spawned++;
    }, [700, 500, 350][Math.min(this.wave - 1, 2)]);
  }
}
```

---

## 六、广告系统实现

### 6.1 广告位配置

```typescript
// services/ad-service.ts

// 在微信公众平台申请以下广告位
const AD_UNITS = {
  reviveVideo:  'adunit-xxxxxxx1',   // 复活激励视频
  energyVideo:  'adunit-xxxxxxx2',   // 体能补充激励视频
  stationBanner: 'adunit-xxxxxxx3',  // 站点结算 Banner
  openInterstitial: 'adunit-xxxxxxx4', // 每日首次打开插屏
};

// 每日广告上限
const AD_LIMITS = {
  revivePerGame: 2,        // 同一关卡复活上限
  energyPerDay: 3,         // 每日体能广告上限
  interstitialPerDay: 1,   // 每日插屏上限
};

export class AdService {
  private videoAd: WechatMiniprogram.RewardedVideoAd | null = null;

  // 创建激励视频广告实例（全局单例）
  initRewardedVideo(unitId: string) {
    if (!wx.createRewardedVideoAd) return;
    this.videoAd = wx.createRewardedVideoAd({ adUnitId: unitId });
    this.videoAd.onError((err) => console.warn('Ad error:', err));
  }

  // 播放激励视频，返回是否完整观看
  showRewardedVideo(): Promise<boolean> {
    return new Promise((resolve) => {
      if (!this.videoAd) { resolve(false); return; }

      this.videoAd.onClose((res) => {
        resolve(res && res.isEnded);
      });

      this.videoAd.show().catch(() => {
        this.videoAd!.load().then(() => this.videoAd!.show())
          .catch(() => resolve(false));
      });
    });
  }

  // 检查今日广告次数
  async canShowAd(type: 'revive' | 'energy'): Promise<boolean> {
    const player = await getPlayerData();
    const today = new Date().toISOString().slice(0, 10);

    if (player.adState.lastAdDate !== today) {
      // 新的一天，重置计数
      return true;
    }

    if (type === 'revive') return player.adState.reviveToday < AD_LIMITS.revivePerGame;
    if (type === 'energy') return player.adState.energyAdToday < AD_LIMITS.energyPerDay;
    return false;
  }
}
```

---

## 七、分享图生成

```typescript
// components/share-card/share-card.ts

export async function generateShareImage(data: {
  stationName: string;
  stationIcon: string;
  km: number;
  elevation: number;
  day: number;
  riderConfig: RiderConfig;
}): Promise<string> {
  // 使用离屏 Canvas 生成分享图
  const canvas = wx.createOffscreenCanvas({
    type: '2d', width: 750, height: 1000
  });
  const ctx = canvas.getContext('2d');

  // 1. 绘制背景渐变
  const bg = ctx.createLinearGradient(0, 0, 0, 1000);
  bg.addColorStop(0, '#0a1628');
  bg.addColorStop(0.6, '#1a365d');
  bg.addColorStop(1, '#2d3748');
  ctx.fillStyle = bg;
  ctx.fillRect(0, 0, 750, 1000);

  // 2. 标题
  ctx.font = 'bold 48px sans-serif';
  ctx.fillStyle = '#4fd1c5';
  ctx.textAlign = 'center';
  ctx.fillText('滇藏骑行', 375, 80);

  // 3. 站点信息
  ctx.font = '72px serif';
  ctx.fillText(data.stationIcon, 375, 200);
  ctx.font = 'bold 36px sans-serif';
  ctx.fillStyle = '#e2e8f0';
  ctx.fillText(data.stationName, 375, 280);

  // 4. 数据卡片
  ctx.font = '28px sans-serif';
  ctx.fillStyle = '#4fd1c5';
  ctx.fillText(`${data.km} km · 海拔 ${data.elevation}m · 第 ${data.day} 天`, 375, 340);

  // 5. 小程序码区域（留白，发布时替换）
  ctx.fillStyle = '#ffffff';
  ctx.fillRect(300, 800, 150, 150);
  ctx.font = '20px sans-serif';
  ctx.fillStyle = '#718096';
  ctx.fillText('扫码加入', 375, 980);

  // 6. 导出为临时路径
  const tempPath = `${wx.env.USER_DATA_PATH}/share_${Date.now()}.png`;
  const imgData = canvas.toDataURL('image/png');
  // ... 保存并返回路径
  return tempPath;
}
```

---

## 八、性能优化策略

| 问题 | 解决方案 |
|------|---------|
| 地图 Canvas 绘制性能 | 只重绘变化部分（脏矩形），静态部分缓存为离屏 Canvas |
| 骑友数据量大 | 分页加载 + 按站点聚合，单次最多返回 100 条 |
| 小关卡掉帧 | `requestAnimationFrame` 驱动，避免 `setInterval`；精灵图代替单独图片 |
| 步数同步频率 | 每次打开 App 同步一次，中途不重复请求 |
| 存档写入 | 本地 Storage 即时写，云数据库延迟合并写（退出/切后台时批量） |
| 冷启动速度 | 分包加载：主包（地图+核心逻辑）< 2MB，关卡独立分包 |
| 图片资源 | 云存储 CDN + 本地缓存，精灵图用 WebP 格式 |

---

## 九、分包策略

```json
// app.json
{
  "pages": [
    "pages/splash/index",
    "pages/customize/index",
    "pages/map/index",
    "pages/riding/index",
    "pages/station/index",
    "pages/profile/index"
  ],
  "subpackages": [
    {
      "root": "games",
      "name": "mini-games",
      "pages": [
        "rock-dodge/index",
        "dog-chase/index",
        "avalanche/index",
        "tire-fix/index",
        "wind-balance/index"
      ]
    }
  ],
  "preloadRule": {
    "pages/map/index": {
      "network": "all",
      "packages": ["mini-games"]
    }
  }
}
```

**包大小预估：**

| 包 | 内容 | 预估大小 |
|----|------|---------|
| 主包 | 页面 + 组件 + 服务 + 基础素材 | ~1.5 MB |
| mini-games 分包 | 5 个小关卡 + 精灵图 | ~1.2 MB |
| 总计 | | ~2.7 MB（限制 20MB）|

---

## 十、开发排期建议

| 阶段 | 周期 | 内容 | 交付物 |
|------|------|------|--------|
| **P0: 基础框架** | 第 1 周 | 项目搭建、云开发环境、数据库 Schema、步数获取 | 能跑通步数 → 体能流程 |
| **P1: 核心循环** | 第 2-3 周 | 地图页面、骑行计算、里程推进、站点系统 | 能从大理骑到丽江 |
| **P2: 关卡系统** | 第 4-5 周 | Canvas 关卡引擎 + 落石/野狗/爆胎 3 个核心关卡 | MVP 可玩 |
| **P3: 社交系统** | 第 6 周 | 骑友位置、站点聚合、排行榜 | 能看到路上的骑友 |
| **P4: 骑手自定义** | 第 7 周 | 形象编辑器、装备系统、成就 | 完整自定义体验 |
| **P5: 变现 & 分享** | 第 8 周 | 广告接入、分享图生成、打卡日历 | 上线准备就绪 |
| **P6: 测试上线** | 第 9-10 周 | 真机调试、性能优化、提审 | 正式上线 |

---

## 十一、微信能力依赖清单

| 能力 | 接口 | 用途 | 需用户授权 |
|------|------|------|-----------|
| 微信运动 | `wx.getWeRunData()` | 获取步数 | 是（scope.werun）|
| 用户信息 | `wx.getUserProfile()` | 昵称、头像 | 是 |
| 激励视频广告 | `wx.createRewardedVideoAd()` | 复活 & 体能补充 | 否 |
| Banner 广告 | `wx.createBannerAd()` | 站点结算页 | 否 |
| 插屏广告 | `wx.createInterstitialAd()` | 每日首次打开 | 否 |
| 分享 | `wx.shareAppMessage()` | 分享里程卡 | 否 |
| 云开发 | `wx.cloud.*` | 全部后端能力 | 否 |
| 本地存储 | `wx.setStorageSync()` | 本地缓存 | 否 |

---

## 十二、风险 & 应对

| 风险 | 影响 | 应对方案 |
|------|------|---------|
| 微信运动授权拒绝 | 无法获取步数，游戏不可玩 | 引导页说明用途 + 手动输入步数兜底 |
| Canvas 2D 在低端机卡顿 | 关卡体验差 | 降级渲染：减少粒子、降低帧率至 30fps |
| 云数据库并发限制 | 骑友数据查询慢 | 热数据缓存 + 定时聚合（每 5 分钟更新位置快照）|
| 广告填充率低 | 收入低于预期 | 备选多家广告联盟，激励视频优先保证填充 |
| 审核被拒 | 延迟上线 | 提前阅读小程序游戏审核规范，关卡不涉及暴力血腥 |

---

*下一步：可基于此方案开始搭建项目脚手架，或先做某个模块的详细设计。*
