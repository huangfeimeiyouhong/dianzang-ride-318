# 滇藏骑行小程序 - 部署运行指南

## 前提条件

- 已安装 [微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)（稳定版）
- 有微信账号（用于扫码登录开发者工具）

## 第一步：打开项目

1. 启动微信开发者工具，用微信扫码登录
2. 选择 **"+"** → **导入项目**
3. 填写：
   - **项目目录**：选择 `dianzang-ride` 文件夹（即包含 `app.json` 的目录）
   - **AppID**：点击 **"测试号"** 按钮（或留空让系统自动分配）
   - **项目名称**：滇藏骑行
4. 点击 **"确定"**

## 第二步：配置开发者工具

导入项目后，点击右上角 **"详情"** → **"本地设置"**，勾选：

- ✅ 不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书
- ✅ 启用 TypeScript 编译（部分版本默认开启）

## 第三步：检查编译状态

打开后开发者工具会自动编译。观察控制台：

- 如果看到 `编译成功`，说明项目可以正常编译
- 如果有报错，检查控制台输出的错误信息
- 预期日志：`[Cloud] 云开发初始化失败，将使用本地存储模式`（测试号下是正常的）

## 第四步：测试核心流程

在模拟器中依次测试：

1. **启动页** (splash) → 应显示游戏 Logo 和启动动画
2. **角色定制** (customize) → 选择骑手形象和昵称
3. **地图页** (map) → 查看大理→拉萨路线，任务面板
4. **骑行页** (riding) → 消耗体能，推进里程
5. **驿站页** (station) → 到站后查看站点信息
6. **档案页** (profile) → 查看成就、装备、统计

## 第五步：真机预览

1. 点击开发者工具顶部工具栏的 **"预览"** 按钮
2. 用手机微信扫描弹出的二维码
3. 在手机上体验完整游戏流程

> 注意：真机上微信运动步数获取需要用户授权，模拟器中无法测试

## 当前模式说明

项目目前运行在 **本地存储模式** 下：
- 玩家数据保存在手机本地（wx.setStorageSync）
- 骑友显示为随机模拟数据
- 步数获取：优先微信运动真实步数，未授权时使用模拟步数
- 所有游戏功能完整可用

## 升级到云开发（可选）

如果你希望启用云开发（多人骑友、数据云端同步），需要：

1. 在微信公众平台注册正式小程序（获取真实 AppID）
2. 在开发者工具中：**云开发** → 开通云开发 → 创建环境
3. 记下环境 ID，替换 `app.ts` 中的 `env: 'dianzang-ride-prod'`
4. 在云开发控制台创建数据库集合：`players`、`rider_positions`
5. 在云开发控制台创建云函数：`getSteps`（微信运动解密）、`getRiders`（骑友查询）
6. 将 `project.config.json` 中添加回 `"cloudfunctionRoot": "cloud/"` 并创建 `cloud/` 目录
